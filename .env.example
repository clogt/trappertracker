# TrapperTracker Environment Variables
# Copy this file to .dev.vars for local development
# For production, use: wrangler secret put <SECRET_NAME>

# ============================================
# REQUIRED SECRETS
# ============================================

# JWT Secret Key - Used for signing authentication tokens
# SECURITY: Must be a strong random string (256+ bits recommended)
# Generate with: openssl rand -base64 32
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-secret-key-here-change-this-to-random-string

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Database ID (for reference only - actual binding is in wrangler.toml)
# DATABASE_ID=your-d1-database-id

# ============================================
# PRODUCTION DEPLOYMENT NOTES
# ============================================

# For production deployment on Cloudflare Workers:
# 1. DO NOT commit .dev.vars or .env files to git
# 2. Set secrets using Wrangler CLI:
#    wrangler secret put JWT_SECRET
# 3. Verify secrets:
#    wrangler secret list
#
# The JWT_SECRET must be the same across all Workers instances
# to ensure token validation works correctly.

# ============================================
# SECURITY BEST PRACTICES
# ============================================

# ✓ Use a cryptographically secure random string
# ✓ Minimum 32 characters (256 bits)
# ✓ Never reuse secrets across environments
# ✓ Rotate secrets periodically (every 90 days recommended)
# ✓ Never commit secrets to version control
# ✓ Use different secrets for dev/staging/production
